export interface PartnerBenefit {
  icon: any;
  title: string;
  description: string;
}

export interface PartnerLink {
  label: string;
  description: string;
  path: string;
  icon: any;
}

export interface Testimonial {
  quote: string;
  name: string;
  role: string;
  initials: string;
}
