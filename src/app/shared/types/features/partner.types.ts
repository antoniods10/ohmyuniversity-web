export interface PartnerBenefit {
  emoji: string;
  title: string;
  description: string;
}

export interface PartnerLink {
  label: string;
  description: string;
  path: string;
  icon: string;
}

export interface Testimonial {
  quote: string;
  name: string;
  role: string;
  initials: string;
}
